# Deterministic CI test dependencies.
#
# These pins reproduce the validated application environment for the
# FastAPI 0.110.0 / Starlette 0.36.3 stack (see requirements.txt).  In
# particular, Starlette 0.36.3's TestClient is incompatible with httpx 0.28+
# (Client() no longer accepts the app= shortcut), so httpx must stay on the
# 0.27.x line.  pytest and pytest-asyncio are pinned to validated releases
# instead of letting pip resolve unconstrained latest versions.

httpx==0.27.2
pytest==8.2.2
pytest-asyncio==0.23.7
