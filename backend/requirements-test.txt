# Deterministic CI test dependencies.
#
# Keep the HTTP client on the stable 0.28 line used by the current FastAPI /
# Starlette stack. pytest and pytest-asyncio remain pinned so CI resolves a
# reproducible test environment.

httpx==0.28.1
pytest==8.2.2
pytest-asyncio==0.23.7
